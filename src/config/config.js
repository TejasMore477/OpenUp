
export const MAX_IMAGES = parseInt(import.meta.env.VITE_MAX_IMAGES);

export const MIN_IMAGES = parseInt(import.meta.env.VITE_MIN_IMAGES);

export const MAX_IMG_SIZE = parseInt(import.meta.env.VITE_MAX_FILE_SIZE_MB);

export const PAGE_PADDING_SIZE = parseInt(import.meta.env.VITE_PAGE_PADDING);

export const CAPTION_HEIGHT_SIZE = parseInt(import.meta.env.VITE_CAPTION_HEIGHT_SPACE);